#include "main.h"

echo $(($(($water#$WATER) + $(($stir#STIR))))
echo $(($(($water#$WATER) + $(($stir#STIR))))
/**
 * A code to print the string _putchar followed by a new line
 * Returns 0
 * 
 */

int main()
{
	_putchar('_putchar\n')
	return (0);
}
